.title
	font-family: 'Roboto Condensed', sans-serif;
	font-size 40px
	color $color
	text-transform uppercase
	font-weight 600
	
	&--join
		font-size 40px
		font-weight 600
		color #fff
	
	@media (max-width: $screen-xs-max)
		font-size 37px